import { ExponetialPipe } from './exponetial.pipe';

describe('ExponetialPipe', () => {
  it('create an instance', () => {
    const pipe = new ExponetialPipe();
    expect(pipe).toBeTruthy();
  });
});
